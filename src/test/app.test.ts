import { solution } from "../app";

describe("Solution", () => {
    it(`Test 1`, () => {
        expect(
            solution([1, 3, 2])
        ).toBe(true);
    });

    it(`Test 2`, () => {
    });
});